doctype html

link(rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'
  integrity='sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u' crossorigin='anonymous')
script.
  const url = 'wss://smart-obd.herokuapp.com/8080'
  const connection = new WebSocket(url);
  connection.onopen = () => {
  connection.send("hey")
  }
  connection.onerror = error => {
  console.log(`WebSocket error: ${error}`)
  }

  connection.onmessage = e => {
  console.log(e.data)
  }
include header.pug
.body
  .container-50
    .jumbotron
      h1.display-4 Live data
        table.table#display
          thead
            tr
              th(scope='col') KMPH
              th(scope='col') RPM
              th(scope='col') Coolant Temp
              th(scope='col') Fuel Level
              th(scope='col') MAF
              th(scope='col') Battery Voltage
          tbody          
            tr
              - console.log("The brackets and quotes "+car_data["RPM"])
              td= car_data["SPEED KMPH"]
              td= car_data["RPM"]
              td= car_data["Coolant Temp"]
              td= car_data["Fuel Level"]
              td= car_data["MAF"]
              td= car_data["voltage"]
              td Testing


include footer.pug
